#include "myutils.h"
#include <time.h>

int main() {
        for(long long i = 3; pfound < 10001 ;i++) {
                isPrime(i);
        }
        printf("%ld\n",primes[pfound-1]);
        return 0;
}

